# Copiar a .env.production ANTES de correr `npm run build`.
#
# Vite incrusta estas variables en el bundle en tiempo de compilación: no se leen
# al ejecutar. Cambiar la URL de la API exige volver a compilar y volver a subir.
VITE_API_URL=https://api.construprec.surcode.cl/api
